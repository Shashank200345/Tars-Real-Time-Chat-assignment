const authConfig = {
    providers: [
        {
            domain: "https://grateful-urchin-74.clerk.accounts.dev",
            applicationID: "convex",
        },
    ],
};

export default authConfig;
